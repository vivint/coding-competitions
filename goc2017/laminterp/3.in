app app (app lam two (app lam exp (app app exp app app exp app app exp app app exp two two two two two) (lam m lam n app n m)) (lam f lam x app f (app f x))) (app add 1) 0
app (app lam mul (lam top (app app app (app lam fix (app fix (lam rec lam top lam val lam counter (app (app app app if (app app gt counter top) (lam x val) (lam x (app app app rec top (app app mul val counter) (app app add counter 1)))) false))) (lam f (app (lam x (app f (lam y (app (app x x) y)))) (lam x (app f (lam y (app (app x x) y))))))) top 1 1)) (lam bot lam top (app app app (app lam fix (app fix (lam rec lam top lam val lam counter (app (app app app if (app app gt counter top) (lam x val) (lam x (app app app rec top (app app add val bot) (app app add counter 1)))) false))) (lam f (app (lam x (app f (lam y (app (app x x) y)))) (lam x (app f (lam y (app (app x x) y))))))) top 0 1))) 1000
